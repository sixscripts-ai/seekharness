# Authz evidence notes

Candidate staging and Finding envelope: `../develop-finding/references/finding-contract.md`.
Single writer is `develop-finding`. Single validator is Evidence ingest. Do not add a second validator. Do not write `findings.v1.json`.

## Domain
`authz` — authorization and object-level access. Remediation only. No exploit steps.

## Staging
Write `scratch/candidates/authz.json` (or an explicit operator path) with `candidate_schema: arena-finding-candidate-v1`.
