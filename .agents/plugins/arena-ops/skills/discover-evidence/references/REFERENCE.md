# Discover Evidence

Run manifest for operator Evidence discovery. Cursor door: `/discover-evidence`.

Finding envelope and candidate shape: `../develop-finding/references/finding-contract.md`.
Single writer: `develop-finding`. Single validator: `backend/agent_arena/evidence.py`.

This skill writes no `findings.v1.json`.
