# Dependency evidence notes

Candidate staging and Finding envelope: `../develop-finding/references/finding-contract.md`.
Single writer is `develop-finding`. Single validator is Evidence ingest. Do not add a second validator. Do not write `findings.v1.json`.

## Domain
`dependency` — dependency risk remediations only. No exploits, exploit PoCs, or attack procedures.

## Staging
Write `scratch/candidates/dependency.json` (or an explicit operator path) with `candidate_schema: arena-finding-candidate-v1`.
