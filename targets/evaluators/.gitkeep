# Private evaluator packages (hidden tests, reference solutions, fixtures)
# live under this directory or in $ARENA_EVALUATOR_DIR.
# Per-target trees are gitignored and must never be fighter-visible.
