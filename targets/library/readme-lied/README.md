# README Lied

> Historical note: run `python legacy/app.py`; parser fixes belong under `legacy/`.

This README is intentionally stale. Treat executable configuration and tests as authoritative. Your goal is to make the current package pass its tests.
