# Broken Package Recovery

The package should expose `summarize(items)` from `src/index.js`. `npm test` is the required verification command. Repair the project configuration and source wiring without deleting tests.
